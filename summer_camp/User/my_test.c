
#include "my_test.h"



rc_info_t rc = rc_Init;
uint8_t   dbus_buf[DBUS_BUFLEN];
static int uart_receive_dma_no_it(UART_HandleTypeDef* huart, uint8_t* pData, uint32_t Size)
{//?????????,??DMA?????UART??
  uint32_t tmp1 = 0;
  tmp1 = huart->RxState;
	//????????tmp1,??UART?????????
	if (tmp1 == HAL_UART_STATE_READY)//??UART????????
	{
		if ((pData == NULL) || (Size == 0))
		{
			return HAL_ERROR;
		}
 
		huart->pRxBuffPtr = pData;
		huart->RxXferSize = Size;
		huart->ErrorCode  = HAL_UART_ERROR_NONE;
 
		HAL_DMA_Start(huart->hdmarx, (uint32_t)&huart->Instance->DR, (uint32_t)pData, Size);
		//??DMA??,????UART?????,??????????
	
		SET_BIT(huart->Instance->CR3, USART_CR3_DMAR);
		//??UART?DMA????
		return HAL_OK;
	}
	else
	{
		return HAL_BUSY;
	}
}


void dbus_uart_init(void)//DBUS?????
{
	__HAL_UART_CLEAR_IDLEFLAG(&DBUS_HUART);//????UART?????,??????UART????????????,??????????
	//???????????????????????????????
	__HAL_UART_ENABLE_IT(&DBUS_HUART, UART_IT_IDLE);//??UART?????,?UART??????????????????,???????
	//???????,????????????????
	uart_receive_dma_no_it(&DBUS_HUART, dbus_buf, DBUS_MAX_LEN);//???????,?DMA???????
}

void rc_callback_handler(rc_info_t *rc, uint8_t *buff)
{//???????????(rc)??,?????????
  rc->ch0 = (buff[0] | buff[1] << 8) & 0x07FF;//?buff[0]?buff[1]?????ch0????,??????11?(???0x07FF???)
  rc->ch0 -= 1024;//?????364?1684,?????????1024,??????0
  rc->ch1 = (buff[1] >> 3 | buff[2] << 5) & 0x07FF;
  rc->ch1 -= 1024;
  rc->ch2 = (buff[2] >> 6 | buff[3] << 2 | buff[4] << 10) & 0x07FF;
  rc->ch2 -= 1024;
  rc->ch3 = (buff[4] >> 1 | buff[5] << 7) & 0x07FF;
  rc->ch3 -= 1024;
  rc->roll = (buff[16] | (buff[17] << 8)) & 0x07FF;  //?????
  rc->roll -= 1024;
 
  rc->sw1 = ((buff[5] >> 4) & 0x000C) >> 2;
  rc->sw2 = (buff[5] >> 4) & 0x0003;//sw1?sw2?????????????
	
  if ((abs(rc->ch0) > 660) || \
      (abs(rc->ch1) > 660) || \
      (abs(rc->ch2) > 660) || \
      (abs(rc->ch3) > 660))
	  
  {
    memset(rc, 0, sizeof(rc_info_t));//????????????660,?????????,??rc??????????
  }		
}

uint16_t dma_current_data_counter(DMA_Stream_TypeDef *dma_stream)
{//??DMA????????????,???????????????????
  return ((uint16_t)(dma_stream->NDTR));
}
 
static void uart_rx_idle_callback(UART_HandleTypeDef* huart)
{
	__HAL_UART_CLEAR_IDLEFLAG(huart);
	//??UART?????,???????????????????
	
	if (huart == &DBUS_HUART)//?????DBUS??
	{
		__HAL_DMA_DISABLE(huart->hdmarx);//??DMA??,???????????????????,????????
 
		if ((DBUS_MAX_LEN - dma_current_data_counter(huart->hdmarx->Instance)) == DBUS_BUFLEN)
		{//???????????,????????????18??,?????????
			rc_callback_handler(&rc, dbus_buf);	//??????????
		}
		__HAL_DMA_SET_COUNTER(huart->hdmarx, DBUS_MAX_LEN);//??DMA????????????,????????????
		__HAL_DMA_ENABLE(huart->hdmarx);//????DMA??,????????
	}
}


void uart_receive_handler(UART_HandleTypeDef *huart)
{//????UART???????????????????????
	if (__HAL_UART_GET_FLAG(huart, UART_FLAG_IDLE) && //??UART?????????,??UART???????????
			__HAL_UART_GET_IT_SOURCE(huart, UART_IT_IDLE))//??UART?????????,???????????,????????
	{
		uart_rx_idle_callback(huart);//?????????,????????
	}
}

